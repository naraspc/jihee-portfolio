# JIHEE PORTFOLIO - 1차 프로토타입 적용 방법

이 폴더는 기존 React + TypeScript + Vite 프로젝트에 그대로 덮어쓸 수 있는
`src`와 `public` 파일을 담고 있습니다. 별도 라이브러리는 필요하지 않습니다.

## 적용 순서

프로젝트 루트가 다음 위치라고 가정합니다.

```text
C:\dev\girlfriend-portfolio
```

### 1. 기존 파일 백업

현재 프로젝트의 `src` 폴더를 다른 이름으로 백업합니다.

```powershell
cd C:\dev\girlfriend-portfolio
Rename-Item src src-backup
```

### 2. 파일 복사

압축을 푼 뒤 이 폴더의 `src`, `public`을 프로젝트 루트로 복사합니다.

`public` 폴더가 이미 있다면 삭제하지 말고 내부 파일을 합쳐도 됩니다.

### 3. 실행

```powershell
npm run dev
```

별도 패키지를 설치하지 않아도 됩니다.

## 현재 버전의 특징

- 원본 PDF의 녹색·종이 질감·큰 타이포그래피를 웹용으로 재해석
- 전화번호와 생년월일은 코드와 이미지에서 제외
- 이메일만 Contact에 표시
- 10개 프로젝트 카드
- 프로젝트 클릭 시 상세 모달과 PDF 페이지 갤러리 표시
- 모바일 반응형 지원
- 외부 폰트와 UI 라이브러리 미사용

## 꼭 확인할 내용

1. PDF의 BIM 교육 과정 기간은 `2022.03 ~ 2021.08`로 역순 표기되어 있어
   초기 웹에서는 제외했습니다.
2. 와드커피는 목차에 누락되어 있습니다. 페이지 배치상 2026으로 임시 분류했으므로
   실제 연도를 확인해 주세요.
3. 샤브올데이는 PDF에 별도의 Overview·발주처·기간·기여도 텍스트가 없어
   확인 가능한 범위만 넣었습니다.
4. 이름 영문 표기는 PDF의 `Hwang Ji Hee`를 그대로 사용했습니다.
5. 현재 프로젝트 이미지는 PDF 각 페이지를 웹용 JPG로 변환한 임시 에셋입니다.
   최종 버전은 사진·도면·렌더링 원본을 따로 받아 교체하는 것이 좋습니다.
6. 원본 PDF에는 전화번호와 생년월일이 포함되어 있으므로 그대로 `public` 폴더에
   올리거나 다운로드 링크를 만들지 마세요.

## 다음 작업

- 여자친구에게 프로젝트별 원본 이미지 받기
- 실제 사용한 폰트 이름 확인
- 문구와 경력 날짜 검수
- 원본 이미지로 갤러리 교체
- 스크롤 애니메이션 추가
- 배포 전 모바일 실기기 테스트
